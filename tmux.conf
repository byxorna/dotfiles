# Switch me back to ^A, thanks
set-option -g prefix C-a
unbind-key C-b
bind-key a send-prefix

# scrolling in the window? maybe? please?
set -g terminal-overrides 'xterm*:smcup@:rmcup@'

# I miss ^A^A
bind-key C-a last-window

# Reload tmux config with r
bind r source-file ~/.tmux.conf

# layout toggling
bind-key Space next-layout
bind-key C-Space previous-layout
bind-key = select-layout tiled

# vim style pane selections
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# synchronize-panes
bind m set -w synchronize-panes

# Options
set-option -sg escape-time 50  # this makes vim fucking awful to use
set-option -g base-index 1
set-option -g default-terminal xterm-256color
set-option -g lock-command vlock
set-window-option -g xterm-keys on  # to make ctrl-arrow, etc. work
#monitor window activity
setw -g monitor-activity on
set -g visual-activity on
#allow auto renaming
setw -g automatic-rename on
# window tab format for unfocused tabs
set-option -g window-status-format '#I:#W'
# window tab format for focused tabs (keep in mind, status-right shows #W)
set-option -g window-status-current-format '#I:#W:#F'
set -g history-limit 10000
set-option -g status-position top
set-option -g set-titles on
set-option -g set-titles-string '#T'

##+ messages:
set-window-option -g mode-bg magenta
set-window-option -g mode-fg black
set-option -g message-bg magenta
set-option -g message-fg black

##+ panes:
set-option -g pane-border-fg black
set-option -g pane-border-bg default
set-option -g pane-active-border-fg white
set-option -g pane-active-border-bg default

##+ status bar:
set-option -g status-utf8 on
set-option -g status-justify left
set-option -g status-bg '#0e0e0e'
set-option -g status-fg '#333333'
set-option -g status-interval 5
set-option -g status-left-length 60
set-option -g status-right-length 600
set-option -g status-left '###[fg=#333333] #h #[default]#[bg=colour243,fg=#333333]#S:#P#[default]#[fg=#555555]#[default]'
set-option -g status-right "#[fg=#555555]#T#[default] [#[fg=colour34,bg=default]#(uptime|awk \\'{print \$\\(NF-2\\),\$\\(NF-1\\),\$NF}\\')#[fg=default,bg=default]] %Y.%m.%d %H:%M:%S"
set-option -g visual-activity on
set-window-option -g monitor-activity on
set-window-option -g window-status-current-bg '#0e0e0e'
#set-window-option -g window-status-current-fg '#3d3a3a'
set-window-option -g window-status-current-fg 'colour25'
set-window-option -g window-status-bg '#0e0e0e'
set-window-option -g window-status-fg '#333333'

##+ clock:
set-window-option -g clock-mode-colour cyan
set-window-option -g clock-mode-style 24
